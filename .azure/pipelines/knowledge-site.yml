trigger:
  branches:
    include:
      - feature/project-setup
  paths:
    include:
      - apps/lms-knowledge-site/**
      - shared/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - apps/lms-knowledge-site/**
      - shared/**

pool:
  name: 'LMS agent pool'

variables:
  - group: lms-swa-tokens   # SWA_TOKEN_KNOWLEDGE_SITE
  - group: lms-build-vars   # API_BASE_URL

steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '22.x'
    displayName: 'Use Node.js 22'

  - task: Cache@2
    inputs:
      key: 'npm | "$(Agent.OS)" | package-lock.json'
      restoreKeys: |
        npm | "$(Agent.OS)"
      path: $(Pipeline.Workspace)/.npm
    displayName: 'Cache npm downloads'

  - script: |
      npm config set cache "$(Pipeline.Workspace)/.npm" --global
      npm ci
    displayName: 'Install workspace dependencies'

  - script: npm run build:knowledge
    displayName: 'Build knowledge site'
    env:
      VITE_API_BASE_URL: $(API_BASE_URL)

  - script: npx --yes @azure/static-web-apps-cli deploy apps/lms-knowledge-site/dist --deployment-token $(SWA_TOKEN_KNOWLEDGE_SITE) --env production
    displayName: 'Deploy to Azure Static Web Apps'
