name: Deploy Learner Site

on:
  push:
    branches:
      - main
      - feature/project-setup
      - feature/create-assements
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and deploy learner site
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.27.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build learner site
        run: pnpm run build:learner
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ format('{0}/api', vars.API_BASE_URL) }}
          NEXT_PUBLIC_ENTRA_CLIENT_ID: ${{ vars.ENTRA_CLIENT_ID_LEARNER }}
          NEXT_PUBLIC_ENTRA_TENANT_ID: ${{ vars.ENTRA_TENANT_ID }}
          NEXT_PUBLIC_API_SCOPE: ${{ vars.API_SCOPE }}

      - name: Build and deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.SWA_TOKEN_LEARNER_APP }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          skip_app_build: true
          app_location: apps/lms-learnersite-dev
          output_location: .next
